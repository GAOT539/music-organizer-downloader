# ── Core UI & Data ────────────────────────────────────────────
streamlit>=1.62.0
pandas>=3.0.5
plotly>=7.0.0

# ── Download engines ──────────────────────────────────────────
# spotdl se instala con --no-deps en el Dockerfile para evitar el conflicto
# uvicorn entre spotdl<4.5 (exige <0.24) y streamlit>=1.62 (exige >=0.30).
# Como solo usamos "spotdl download" vía subprocess (nunca su web server),
# la versión de uvicorn que decide streamlit es segura.
yt-dlp>=2026.8.19

# Dependencias runtime de spotdl (instaladas aquí sin restricción de uvicorn)
spotipy>=2.26.0
ytmusicapi>=1.7.0
beautifulsoup4>=4.12.0
pydantic>=2.9.2,<3
rich>=13.9.4,<14
rapidfuzz>=3.10.1,<4
python-slugify[unidecode]>=8.0.4,<9
platformdirs>=4.3.6,<5
pykakasi>=2.3.0,<3
soundcloud-v2>=1.6.0,<2
fastapi>=0.103.0,<0.116

# ── Audio metadata & lyrics ───────────────────────────────────
mutagen>=1.48.1
syncedlyrics>=1.0.1

# ── Utilities ─────────────────────────────────────────────────
requests>=2.34.2
Pillow>=12.3.0
uvicorn>=0.30.0